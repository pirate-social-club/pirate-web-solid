import { RoutePlaceholder } from "../features/shell/route-placeholder.tsx";

export default function SearchRoute() {
  return <RoutePlaceholder description="Search isn't available yet. Find communities from the menu and songs from the home feed." path="/search" title="Search" />;
}
