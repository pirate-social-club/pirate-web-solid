import { RoutePlaceholder } from "../features/shell/route-placeholder.tsx";

export default function SearchRoute() {
  return <RoutePlaceholder activeItemId="search" description="Search creators, communities, posts, and sounds." path="/search" title="Search" />;
}
