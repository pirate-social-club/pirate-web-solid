import { RoutePlaceholder } from "../features/shell/route-placeholder.tsx";

export default function LiveRoute() {
  return <RoutePlaceholder activeItemId="live" description="Live rooms and broadcasts will land here." path="/live" title="Live" />;
}
