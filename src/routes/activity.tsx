import { RoutePlaceholder } from "../features/shell/route-placeholder.tsx";

export default function ActivityRoute() {
  return <RoutePlaceholder activeItemId="activity" description="Your follows, replies, mentions, and community activity will appear here." path="/activity" title="Activity" />;
}
