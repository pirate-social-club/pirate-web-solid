# Application workspace root

The standalone Solid application currently lives at the repository root. This
directory remains empty so the shared, byte-identical dependency-policy test can
scan the conventional `apps/` and `packages/` workspace roots without carrying
a Solid-specific fork.
