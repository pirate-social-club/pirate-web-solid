# Contextual adult viewing

The final local client adopts immutable api-client 0.79.0, artifact SHA-256
`85488f318432c4ee3e124f16be406acc4a762c4953fc8f1cc58f0a2cc6e19c27`, and uses the same document
provider host as nationality. Fresh account and server age authority checks
control every ceremony poll. Sign-in can happen in place; cancellation, account
change and disposed components cannot accept late proof results. No local
boolean unlocks content: proof is followed by a fresh guarded read.

The shared content-free prompt is integrated into homepage and card feeds,
community posts, post detail, comment threads, study, karaoke and adult video
renewal. Loaded pages and drafts are retained where applicable. A stale page
response cannot restore content after account change. Public discovery remains
credential-free; post-proof refresh uses the authenticated home loader. Video
renewal and verification return do not automatically play, join, claim, start a
lesson, or enable a microphone. The combined API candidate supplies the adult
access hint used by renewed delivery prompts.

At final commit `8fbbcd9f6fde8f7db5d2c71e48d587cf7efc7a9d`, `bun run verify`
passed its 149 API transport tests, 820 app tests, 20 SSR tests, type checks,
lint, provenance, runtime-table, and dependency stages. Its final parallel
design-system stage timed out in nine cases under host contention, so the whole
command exited nonzero and is retained as failed evidence. The unchanged
design-system suite then passed 409 tests across 72 files with one worker.
`bun run build-storybook` and `bun run build` both passed separately at the
same commit. Existing DOM-emulation and bundle-size warnings remain visible in
the logs.

Browser assertions against the final built catalog proved that the real
community-create action submits, Palm remains required, nationality authoring
selects countries, and the copy presents Self and ZKPassport as equal choices.
A locked desktop adult feed contains no video element. The authorized fixture
contains a video but remains paused at time zero, and cancellation at 390 by
844 keeps the content locked with no video element. The default handle
storefront permits anonymous continuation into Pirate. Final screenshots are
retained. The first two historical development-preview attempts displayed a
blank page and timed out without a browser exception; they are not passed
evidence. The built catalog succeeded.

These proofs use local fixtures, including proof outcomes. They are not real
Self or ZKPassport document acceptance and do not establish deployed cache
invalidation or remote return behavior. No deployment, push, capability
enablement, cache purge, or retained-content reconciliation occurred.
Compressed logs are sanitized text, `source-manifest.json` pins the tested
source and artifact, and `SHA256SUMS` covers this evidence directory.
