# Contextual adult viewing

The local client adopts immutable api-client 0.78.0 and uses the same document
provider host as nationality. Fresh account and server age authority checks
control every ceremony poll. Sign-in can happen in place; cancellation, account
change and disposed components cannot accept late proof results. No local
boolean unlocks content: proof is followed by a fresh guarded read.

The shared content-free prompt is integrated into homepage and card feeds,
community posts, post detail, comment threads, study, karaoke and adult video
renewal. Loaded pages and drafts are retained where applicable. A stale page
response cannot restore content after account change. Public discovery remains
credential-free; post-proof refresh uses the authenticated home loader. Video
renewal and verification return do not automatically play, join, claim, start a
lesson, or enable a microphone. API f411d420 supplies the adult access hint used
by renewed delivery prompts.

The final full app run passed 820 tests across 92 files; SSR passed 20 and the
dependency audit returned an empty object. The earlier 149-test API run passed
on the same client and transport files. The serial design-system run passed
409 tests across 72 files after fixing the story's placeholder union. That
serial command later failed lint on a new module mock; the mock was replaced
with explicit dependency injection, and detail proof plus lint passed. The
final built-browser command passed types, 33 focused tests, lint, Storybook
build, browser assertions and the production Worker build. These are component
commands, not a claimed whole verify command. Existing lint and bundle-size
warnings remain visible in the logs.

Browser assertions against the built catalog proved a locked desktop feed has
no video, successful fixture proof refreshes without autoplay, and cancellation
at 390 by 844 keeps content locked. Screenshots are retained. The first two
development-preview attempts displayed a blank page and timed out without a
browser exception; they are not passed evidence. The built catalog succeeded.
A previous UI run under one CPU started too many workers, timed out and was
stopped; the successful rerun used one worker. Initial test discovery omitted
two new files; the registry was fixed before the final full app run.

These proofs use local fixtures, including proof outcomes. They are not real
Self or ZKPassport document acceptance, and do not establish every remote return
or delivery/cache surface. No deployment, push or capability enablement occurred.
Historical rating reconciliation, metadata and final cross-system acceptance
remain open. Compressed logs are sanitized text, source-manifest.json pins the
tested files, and SHA256SUMS covers this evidence directory.
